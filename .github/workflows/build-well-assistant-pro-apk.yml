name: Build Well Assistant Pro APK
on:
  workflow_dispatch:
  push:
    paths:
      - 'well-assistant-pro-android/**'
      - 'tools/patch_well_assistant_real_m32.py'
      - 'tools/patch_well_assistant_channel_autodetect.py'
      - 'tools/patch_well_assistant_live_vu_autoconnect.py'
      - 'tools/fix_m32_meter_string_type.py'
      - 'tools/patch_well_assistant_real_overview_meter.py'
      - '.github/workflows/build-well-assistant-pro-apk.yml'
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply Real M32 connect UI
        run: python3 tools/patch_well_assistant_real_m32.py
      - name: Apply channel setup + auto detect fix
        run: python3 tools/patch_well_assistant_channel_autodetect.py
      - name: Apply live VU + auto connect
        run: python3 tools/patch_well_assistant_live_vu_autoconnect.py
      - name: Fix M32 meter OSC request
        run: python3 tools/fix_m32_meter_string_type.py
      - name: Use only real M32 overview meter values
        run: python3 tools/patch_well_assistant_real_overview_meter.py
      - name: Save requested fixes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add well-assistant-pro-android/app/src/main/assets/index.html well-assistant-pro-android/app/src/main/java/com/wellsound/assistantpro/MainActivity.java tools/patch_well_assistant_live_vu_autoconnect.py
          if git diff --cached --quiet; then
            echo 'Requested fixes already applied'
          else
            git commit -m 'Use real M32 input meters in overview and remove fake levels [skip ci]'
            git push
          fi
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'
      - name: Build debug APK
        working-directory: well-assistant-pro-android
        run: gradle assembleDebug --stacktrace
      - name: Rename APK
        run: cp well-assistant-pro-android/app/build/outputs/apk/debug/app-debug.apk Well-Assistant-Pro-M32-RealOverviewMeter.apk
      - uses: actions/upload-artifact@v4
        with:
          name: Well-Assistant-Pro-M32-RealOverviewMeter
          path: Well-Assistant-Pro-M32-RealOverviewMeter.apk
# build: real-overview-meter-v1
